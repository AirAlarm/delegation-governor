[project]
name = "delegation-governor"
version = "0.3.0"
description = "Multi-endpoint task orchestration for Claude Desktop: Codex, local, Oracle, and OpenRouter workers."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
dg = "dg.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dg"]
