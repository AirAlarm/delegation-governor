[project]
name = "delegation-governor"
version = "0.1.0"
description = "Supervisor/worker/task orchestration for Claude Code: Codex-first delegation, cc-delegate fallback, LM Studio supervisor failover."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
dg = "dg.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dg"]
